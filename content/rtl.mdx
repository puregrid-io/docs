---
title: RTL (Right to left)
description: Learn how to set the grid up for RTL languages
---

import { Showcase } from '../src/components';
import { DirectionRtl } from '../examples';

The grid defaults to LTR (left to right), you can change this with the `direction={Direction.Rtl}` prop for RTL (right to left) languages.

<Showcase fileName="DirectionRtl.tsx">
  <DirectionRtl />
</Showcase>
